extends baseTemplate

block content 
  h1 This is user lists Overview
  div
    table.table-header
      thead
        tr
          th(scope='col') name
          th(scope='col') email
          th(scope='col') role
      tbody
        each user in users
          tr.table-secondary
            td=user.name
            td=user.email
            td=user.role